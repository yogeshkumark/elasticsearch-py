---
- job:
    project-type: freestyle
    node: linux && immutable
    name: elastic+clients+benchmarks+elasticsearch-py
    display-name: 'elastic / benchmarks # elasticsearch-py'
    description: Running client benchmarks for the Python client
    parameters:
      - string: { name: branch_specifier, default: refs/heads/benchmarks }
    triggers:
      - timed: "H H/12 * * *"
    vault:
      role_id: "ddbd0d44-0e51-105b-177a-c8fdfd445126"
    builders:
      - shell: |-
          #!/usr/local/bin/runbld
          .ci/run-benchmarks
